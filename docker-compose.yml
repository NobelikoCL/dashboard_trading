services:
  backend:
    build: ./backend
    env_file: .env
    restart: unless-stopped
    ports:
      - "0.0.0.0:8000:8000"
  frontend:
    build: ./frontend
    depends_on:
      - backend
    restart: unless-stopped
    ports:
      - "0.0.0.0:5173:5173"
